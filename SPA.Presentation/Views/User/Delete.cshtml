@using SPA.Domain.Dtoes
@model DeleteUserDto
@{
    ViewData["Title"] = "حذف کاربر";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-6">
            <form id="deleteUserForm" method="post" style="text-align:center">
                <h2>آیا از حذف اطمینان دارید ؟</h2>
                <input type="hidden" asp-for="Id" />
                <div class="input-group2">
                    <label asp-for="@Model.Name" class="control-label"></label>
                    <input asp-for="@Model.Name" class="form-control" readonly />
                </div>
                <div class="input-group2">
                    <label asp-for="@Model.Family" class="control-label"></label>
                    <input asp-for="@Model.Family" class="form-control" readonly />
                </div>
                <div class="form-group d-flex justify-content-between mt-3">
                    <button type="button" class="btn btn-danger" onclick="DeleteUser()" style="width: 48%;">حذف</button>
                    <button type="button" class="btn btn-secondary" onclick="goBack()" style="width: 48%;">بازگشت</button>
                </div>
            </form>
        </div>
    </div>
</div>